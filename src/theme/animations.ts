import { FadeIn, FadeInUp, ZoomIn } from 'react-native-reanimated';

export const screenTransition = FadeIn.duration(200);

export const resultRevealTransition = FadeInUp.duration(280);

export const championRevealTransition = ZoomIn.duration(320);
